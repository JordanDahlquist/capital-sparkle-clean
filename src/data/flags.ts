// Shared feature flags — flip once the client confirms the underlying facts.
// Keeping these in one file ensures the hero trust strip, footer badge, and
// FAQ item (plus its JSON-LD entry) all stay in sync.
export const SHOW_LICENSED_INSURED = true;